
<!--@using Domain
@model Registration


@{
    ViewBag.Title = "Home Page";
}
<div class="container">
    <h2>Add New User</h2>
    <input type="button" class="btn btn-primary" id="newUser" value="newUser" onclick="newUser();" />
    <br /><br />
    <table class="table table-bordered table-hover">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserId)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.UserMobile)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserPass)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Gender)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CounterID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RoleID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedAt)

            </th>


            <th>Action</th>
        </tr>
       @@foreach (var i in Model)
        {
            <tr>
                <td style="display:none;">
                    @@Html.DisplayFor(modelItem => i.UserId)
                </td>
                <td>
                    @@Html.DisplayFor(modelItem => i.UserMobile)
                </td>
                <td>
                   @@Html.DisplayFor(modelItem => i.UserPass)
                </td>
                <td>
                   @@Html.DisplayFor(modelItem => i.Name)
                </td>
                <td>
                   @@Html.DisplayFor(modelItem => i.Gender)
                </td>
                <td>
                   @@Html.DisplayFor(modelItem => i.CounterID)
                </td>
                <td>
                   @@Html.DisplayFor(modelItem => i.RoleID)
                </td>
                <td>
                    @@Html.DisplayFor(modelItem => i.CreatedAt)
                </td>-->
                @*<td>
                        <a class="btn btn-info btn-sm" id="@i.ID" onclick="GetbyID(this.id)">Edit</a>
                        <a class="btn btn-danger btn-sm" id="@i.ID" onclick="GetbyDelID(this.id)"> Delete </a>
                    </td>*@
            <!--</tr>
        }
    </table>
</div>
<div class="modal fade" id="myModal" role="dialog">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 class="modal-title"> User Data</h4>
            </div>
            <div class="modal-body">
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>
<script>
function newUser() {
            $.post("@Url.Action("CreateUser", "Registration")",
            function (data) {
            $('.modal-body').html(data);
            });
            $("#myModal").modal("show"); 
            }
function GetbyID(ID) {
            $.get("@Url.Action("GetByID", "Registration")/" + ID,
            function (data) {
            $('.modal-body').html(data);
            });
            $("#myModal").modal("show");
    }
    </script>-->